# 🛠️ 개발 자동화 스킬 (5개)

Claude Code의 개발 워크플로우를 자동화하는 스킬들입니다.

---

## 1️⃣ code-changelog

### 역할
Git 커밋 히스토리를 분석하여 변경사항을 자동으로 문서화하고 HTML 뷰어로 시각화합니다.

### 주요 기능
- ✅ Git 커밋 히스토리 자동 분석
- ✅ 변경사항을 구조화된 문서로 변환
- ✅ HTML 뷰어로 시각적 표시
- ✅ 날짜별/파일별 그룹핑

### 사용 방법
```bash
code-changelog
```

### 설정
```bash
# Git 저장소가 있는 프로젝트에서 실행
cd /path/to/your/project
code-changelog

# 자동으로 다음 작업 수행:
# 1. git log 분석
# 2. 변경사항 추출
# 3. changelog.html 생성
```

### 출력 예시
```html
<!DOCTYPE html>
<html>
<head>
    <title>Changelog - Your Project</title>
</head>
<body>
    <h1>📝 Changelog</h1>
    <div class="commit">
        <h2>2024-11-02</h2>
        <h3>Add user authentication</h3>
        <ul>
            <li>auth/login.ts: Added JWT token generation</li>
            <li>auth/middleware.ts: Added auth middleware</li>
            <li>routes/user.ts: Protected user routes</li>
        </ul>
    </div>
</body>
</html>
```

### 활용 시나리오
1. **릴리즈 노트 작성**: 버전 배포 전 자동으로 변경사항 정리
2. **팀 공유**: 주간 개발 내용을 HTML로 공유
3. **문서화**: 프로젝트 이력 관리

---

## 2️⃣ meta-prompt-generator

### 역할
반복적인 작업을 자동화하는 커스텀 슬래시 커맨드를 자동 생성합니다.

### 주요 기능
- ✅ 자연어로 작업 설명
- ✅ AI가 슬래시 커맨드 스크립트 자동 생성
- ✅ `~/.claude/commands/` 폴더에 자동 저장
- ✅ 즉시 사용 가능

### 사용 방법
```bash
meta-prompt-generator
```

### 대화형 프로세스
```
AI: 어떤 작업을 자동화하고 싶으신가요?

사용자: "프로젝트를 빌드하고 테스트를 실행한 뒤 결과를 정리해줘"

AI: 알겠습니다. /build-and-test 커맨드를 생성합니다.

✅ 생성 완료: ~/.claude/commands/build-and-test.md

이제 다음과 같이 사용할 수 있습니다:
/build-and-test
```

### 생성된 커맨드 예시
```markdown
# ~/.claude/commands/build-and-test.md

You are a build and test automation assistant.

When the user runs /build-and-test, perform the following:

1. Run the build command:
   - npm run build (for Node.js projects)
   - cargo build (for Rust projects)
   - go build (for Go projects)

2. If build succeeds, run tests:
   - npm test
   - cargo test
   - go test

3. Summarize results:
   - Build time
   - Test results (passed/failed)
   - Any errors or warnings

4. Create a summary report
```

### 활용 시나리오
1. **CI/CD 워크플로우**: 빌드→테스트→배포 자동화
2. **코드 리뷰**: 자동 린트→포맷→검증
3. **문서 생성**: README→API 문서→가이드 자동 생성
4. **데이터 처리**: ETL 파이프라인 자동화

### 유용한 커맨드 예시
```bash
# 예시 1: 풀스택 개발 워크플로우
/fullstack-dev
→ 프론트엔드 빌드 + 백엔드 빌드 + 통합 테스트

# 예시 2: 문서 동기화
/sync-docs
→ 코드 주석 → API 문서 → README 업데이트

# 예시 3: 배포 준비
/prepare-release
→ 버전 업데이트 → 빌드 → 태그 생성 → changelog 생성
```

---

## 3️⃣ prompt-enhancer

### 역할
간단한 요청을 상세하고 구체적인 요구사항으로 자동 확장합니다.

### 주요 기능
- ✅ 모호한 요청을 구체화
- ✅ 누락된 고려사항 자동 추가
- ✅ 보안, UX, 에러처리 자동 포함
- ✅ 베스트 프랙티스 적용

### 사용 방법
```bash
prompt-enhancer
```

### 변환 예시

#### 예시 1: 로그인 기능

**입력 (간단한 요청):**
```
로그인 기능 만들어줘
```

**출력 (상세 요구사항):**
```markdown
# 로그인 기능 구현 요구사항

## 1. 인증 방식
- 이메일/비밀번호 인증
- 소셜 로그인 (Google, GitHub, Kakao)
- JWT 토큰 기반 세션 관리
- Refresh Token 구현

## 2. 보안
- 비밀번호 해싱 (bcrypt, 최소 10 rounds)
- CSRF 토큰 보호
- Rate limiting (5회 실패 시 계정 잠금)
- 2단계 인증 (OTP/SMS) 옵션
- HTTPS only 쿠키
- XSS/SQL Injection 방어

## 3. UX
- 로딩 상태 표시 (스피너)
- 명확한 에러 메시지
- "비밀번호 찾기" 링크
- "로그인 상태 유지" 체크박스
- 자동 완성 지원
- 접근성 (ARIA labels)

## 4. 에러 처리
- 잘못된 자격 증명
- 계정 잠김 (30분 후 재시도)
- 네트워크 에러 (재시도 로직)
- 서버 에러 (500/503)
- 이메일 미인증 계정

## 5. 데이터베이스
- users 테이블 스키마:
  - id (UUID)
  - email (unique, indexed)
  - password_hash
  - created_at
  - updated_at
  - last_login_at
  - failed_login_attempts
  - locked_until

## 6. API 엔드포인트
POST /api/auth/login
POST /api/auth/logout
POST /api/auth/refresh
POST /api/auth/forgot-password
POST /api/auth/reset-password

## 7. 테스트
- 단위 테스트 (인증 로직)
- 통합 테스트 (API)
- E2E 테스트 (로그인 플로우)
- 보안 테스트 (penetration testing)
```

#### 예시 2: 파일 업로드

**입력:**
```
파일 업로드 기능 추가해줘
```

**출력:**
```markdown
# 파일 업로드 기능 요구사항

## 1. 파일 타입
- 허용 타입: image/*, .pdf, .docx, .xlsx
- 최대 파일 크기: 10MB (이미지), 50MB (문서)
- MIME 타입 검증

## 2. 저장소
- 클라우드 스토리지 (AWS S3 / Google Cloud Storage)
- CDN 연동
- 파일명 UUID 변환 (보안)
- 폴더 구조: /uploads/{year}/{month}/{uuid}.ext

## 3. UX
- 드래그 앤 드롭 지원
- 프로그레스 바
- 멀티 파일 업로드
- 미리보기 (이미지)
- 취소 기능

## 4. 보안
- 파일 크기 제한
- MIME 타입 검증
- 바이러스 스캔 (ClamAV)
- 악성 코드 검사
- 접근 권한 관리

## 5. 성능
- Chunked upload (대용량 파일)
- 이미지 자동 리사이징
- WebP 변환 (브라우저 지원 시)
- Lazy loading

## 6. API
POST /api/files/upload
GET /api/files/{id}
DELETE /api/files/{id}
```

### 활용 시나리오
1. **요구사항 정의**: 클라이언트 요청을 상세 스펙으로 변환
2. **코드 리뷰**: 누락된 엣지 케이스 발견
3. **학습**: 베스트 프랙티스 습득

---

## 4️⃣ codex-claude-loop

### 역할
Claude와 OpenAI Codex가 서로 코드를 검증하는 이중 AI 검증 시스템입니다.

### 주요 기능
- ✅ 두 AI의 교차 검증
- ✅ 버그 발견율 향상
- ✅ 코드 품질 보증
- ✅ 다양한 관점 제공

### 사용 방법
```bash
codex-claude-loop
```

### 필수 설정
```bash
# OpenAI API 키 필요
export OPENAI_API_KEY="your-api-key-here"
```

### 검증 프로세스
```
1️⃣ Claude 분석
   ↓
   "이 코드는 메모리 누수 가능성이 있습니다"

2️⃣ Codex 검증
   ↓
   "Claude의 지적이 맞습니다.
    Line 45에서 이벤트 리스너를 제거하지 않았습니다."

3️⃣ Claude 추가 개선안
   ↓
   "useEffect cleanup 함수를 추가하겠습니다."

4️⃣ Codex 최종 검토
   ↓
   "개선안이 적절합니다.
    추가로 AbortController를 사용하면 더 좋습니다."

5️⃣ 최종 합의
   ↓
   "두 AI가 합의한 최적 코드 생성"
```

### 실제 예시

**원본 코드:**
```javascript
function fetchData() {
  fetch('/api/data')
    .then(res => res.json())
    .then(data => setData(data))
}

useEffect(() => {
  fetchData()
}, [])
```

**Claude 분석:**
```
⚠️ 문제점:
1. 에러 처리 없음
2. 로딩 상태 없음
3. 컴포넌트 unmount 시 메모리 누수
4. 의존성 배열 불완전
```

**Codex 추가 지적:**
```
⚠️ 추가 문제:
5. Race condition 가능성
6. 취소 로직 없음
7. 타입 안정성 없음
```

**최종 개선 코드:**
```typescript
function fetchData(signal: AbortSignal) {
  return fetch('/api/data', { signal })
    .then(res => {
      if (!res.ok) throw new Error('API Error')
      return res.json()
    })
}

useEffect(() => {
  const abortController = new AbortController()
  let isMounted = true

  setLoading(true)

  fetchData(abortController.signal)
    .then(data => {
      if (isMounted) {
        setData(data)
        setError(null)
      }
    })
    .catch(err => {
      if (err.name !== 'AbortError' && isMounted) {
        setError(err.message)
      }
    })
    .finally(() => {
      if (isMounted) setLoading(false)
    })

  return () => {
    isMounted = false
    abortController.abort()
  }
}, []) // 의존성 명확
```

### 활용 시나리오
1. **중요한 코드**: 결제, 인증 등 핵심 로직
2. **복잡한 알고리즘**: 성능이 중요한 부분
3. **보안 코드**: 취약점 검증

---

## 5️⃣ codex

### 역할
OpenAI Codex를 활용한 코드 분석 및 리팩토링 도구입니다.

### 주요 기능
- ✅ 레거시 코드 분석
- ✅ 리팩토링 제안
- ✅ 성능 최적화
- ✅ 보안 취약점 탐지

### 사용 방법
```bash
codex
```

### 필수 설정
```bash
export OPENAI_API_KEY="your-api-key-here"
```

### 분석 항목

#### 1. 코드 복잡도
```
✅ Cyclomatic Complexity: 5 (Good)
⚠️ Cognitive Complexity: 15 (High)
❌ Nesting Depth: 4 (Too Deep)

권장: 함수 분리 필요
```

#### 2. 성능 최적화
```
⚠️ O(n²) 알고리즘 발견
   Line 45-60: 중첩 루프

💡 제안:
   Map/Set 자료구조 사용으로 O(n) 개선 가능
```

#### 3. 보안 취약점
```
❌ SQL Injection 위험
   Line 32: 직접 쿼리 조합

💡 제안:
   Prepared Statement 사용
```

#### 4. 베스트 프랙티스
```
⚠️ 하드코딩된 값
   Line 12: const API_KEY = "abc123"

💡 제안:
   환경 변수 사용: process.env.API_KEY
```

### 활용 시나리오
1. **레거시 코드 개선**: 오래된 코드베이스 분석
2. **코드 리뷰**: 자동화된 검토
3. **기술 부채 해결**: 문제점 우선순위화

---

## 💡 5개 스킬 조합 활용

### 워크플로우 1: 완벽한 코드 품질
```bash
1. prompt-enhancer      # 요구사항 구체화
2. (코드 작성)
3. codex               # 1차 분석
4. codex-claude-loop   # 2차 이중 검증
5. code-changelog      # 변경사항 문서화
6. meta-prompt-generator # 이 과정을 /quality-check 커맨드로 저장
```

### 워크플로우 2: 레거시 코드 리팩토링
```bash
1. codex               # 현재 코드 분석
2. prompt-enhancer     # 개선 방향 구체화
3. (리팩토링 진행)
4. codex-claude-loop   # 개선 코드 검증
5. code-changelog      # 리팩토링 내역 문서화
```

---

## 📞 문제 해결

### codex 스킬이 작동하지 않아요
```bash
# OpenAI API 키 확인
echo $OPENAI_API_KEY

# 키가 없다면 설정
export OPENAI_API_KEY="your-key"

# ~/.bashrc 또는 ~/.zshrc에 영구 저장
echo 'export OPENAI_API_KEY="your-key"' >> ~/.bashrc
```

### changelog가 생성되지 않아요
```bash
# Git 저장소 확인
git status

# Git이 없다면 초기화
git init
git add .
git commit -m "Initial commit"
```

---

**다음 문서**: [02_프로젝트초기화_스킬.md](./02_프로젝트초기화_스킬.md)
